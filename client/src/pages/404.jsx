import React, { Fragment } from "react";
import Navbar from "../components/Navbar";

const Fallback = () => {
  return (
    <Fragment>
      <Navbar />
      Fallback Page
    </Fragment>
  );
};

export default Fallback;
